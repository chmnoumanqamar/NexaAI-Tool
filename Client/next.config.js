/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Hides the black "N" dev-mode indicator badge that Next.js shows
    // in the corner of the page while running `npm run dev`.
    devIndicators: false,
};

module.exports = nextConfig;